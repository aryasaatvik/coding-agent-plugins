---
description: Regenerate the session title in the background (applies with your next message)
disable-model-invocation: true
---

The session-title hook normally intercepts `/title` before it reaches you. Reply with exactly: "session-title hook did not run; check that the session-title plugin is enabled."
