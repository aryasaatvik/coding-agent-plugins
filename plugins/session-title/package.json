{
  "name": "@coding-agent-plugins/session-title",
  "version": "1.0.0",
  "description": "Session titles for Claude Code (/title, in the background via OpenCode) and OpenCode (native title hook)",
  "author": "Saatvik Arya",
  "license": "MIT",
  "keywords": [
    "session",
    "title",
    "opencode"
  ],
  "private": true,
  "type": "module",
  "scripts": {
    "build:claude": "bun build ./claude-code/hook.ts --target bun --format esm --outfile ./hooks/session-title-hook.js && bun build ./claude-code/generate.ts --target bun --format esm --outfile ./hooks/generate.js",
    "build:opencode": "bun build ./opencode/session-title-plugin.ts --target bun --format esm --outfile ./opencode/session-title-plugin.js",
    "build": "bun run build:claude && bun run build:opencode",
    "test": "bun test"
  },
  "devDependencies": {
    "@opencode/plugin": "^2.0.14",
    "@types/bun": "latest"
  }
}
