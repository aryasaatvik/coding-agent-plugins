{
  "description": "Steers worktree creation (git worktree add, EnterWorktree without path, Agent isolation: worktree) toward `wt new`, which runs the repo's post-install setup and links the shared Scratchpad",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash|EnterWorktree|Agent|Task",
        "hooks": [
          {
            "type": "command",
            "command": "bun ${CLAUDE_PLUGIN_ROOT}/hooks/wt-hook.js",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
