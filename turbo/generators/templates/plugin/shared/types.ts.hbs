/**
 * Shared TypeScript types for {{ dashCase name }}
 */

/**
 * Plugin configuration options
 */
export interface Config {
  /**
   * Enable or disable the plugin entirely
   * @default true
   */
  enabled?: boolean;

  /**
   * Enable debug logging
   * @default false
   */
  debug?: boolean;
}
