{
  "name": "coding-agent-plugins",
  "version": "1.0.0",
  "description": "Collection of plugins for Claude Code and OpenCode",
  "author": "Saatvik Arya",
  "license": "MIT",
  "keywords": [
    "claude-code",
    "opencode",
    "plugins",
    "development-tools",
    "ni",
    "package-manager"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/aryasaatvik/coding-agent-plugins"
  },
  "scripts": {
    "install-plugins": "bun packages/cli/bin/cli.js",
    "build": "turbo run build",
    "test": "turbo run test",
    "dev": "turbo run dev --parallel",
    "generate": "turbo gen",
    "clean": "turbo run clean && rm -rf .turbo",
    "typecheck": "tsc --noEmit -p ."
  },
  "devDependencies": {
    "@turbo/gen": "^2.11.3",
    "@types/bun": "latest",
    "turbo": "^2.11.3",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "@clack/prompts": "^1.8.1"
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "packageManager": "bun@1.3.2",
  "type": "module",
  "private": true,
  "workspaces": [
    "plugins/*",
    "packages/*"
  ]
}
